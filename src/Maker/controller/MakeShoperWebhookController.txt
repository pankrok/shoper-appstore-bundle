The <info>%command.name%</info> command generates a new Shoper appstore controller class.

<info>php %command.full_name% CoolStuffController</info>

If the argument is missing, the command will ask for the controller class name interactively.

You can also insert webhook secret or add it later in controller:
